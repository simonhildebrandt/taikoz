<div id="venue_select">
  <% fields_for :event, @session do |f| %>
  <%= casein_select f, f.object, :location_id, options_for_select(location_options, selected: f.object.location_id) %>
  <% end %>
</div>
